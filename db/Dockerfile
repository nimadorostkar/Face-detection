# PostgreSQL with pgvector extension
FROM pgvector/pgvector:pg15

# The base image already includes pgvector extension
# No additional setup needed

# Default PostgreSQL environment variables are handled by docker-compose.yml

